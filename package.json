{
  "name": "rate-limiter",
  "version": "1.0.0",
  "description": "Standalone library for rate limiting and throttling",
  "main": "dist/index.js",
  "scripts": { "build": "tsc" },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.2"
  }
}
